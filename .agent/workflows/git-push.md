---
description: Stage all changes, commit with a message, and push to GitHub
---

// turbo-all

1. Run `git add .` in the root of the project (CWD: `d:\INVENTORY MANAGEMENT SYSTEM`)
2. Run `git commit -m "<message>"` — use the user's provided message, or default to `"update"` if none given
3. Run `git push` in the same directory
